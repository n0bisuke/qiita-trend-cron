{
  "name": "quickstart",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "start": "bun qiita-ss.ts",
    "deploy": "git add . && git commit -m 'deploy' && git pull origin main && git push origin main"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "puppeteer": "^21.7.0"
  }
}